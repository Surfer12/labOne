import java.util.LinkedList;
        import java.util.Stack;
        
        public class ExampleClass {
            public static void main(String[] args) {
                Stack<Type> stackName = new Stack<>();
                stackName.push(element); // Push element onto the stack
                Type topElement = stackName.peek(); // Peek at the top element of the stack
                Type poppedElement = stackName.pop(); // Pop element from the stack
                boolean isEmpty = stackName.isEmpty(); // Check if the stack is empty
                int size = stackName.size(); // Get the size of the stack
                stackName.clear(); // Clear the stack
            }
        }
